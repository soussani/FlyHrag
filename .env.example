# Optional only. Default live provider is adsb.fi (personal non-commercial use).
# OpenSky operational integration requires prior WRITTEN permission from OpenSky.
# Do not enable just because anonymous network access works. Never put secrets here.
VITE_OPENSKY_PERMISSION_CONFIRMED=false
